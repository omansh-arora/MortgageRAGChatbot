[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[volumes]
[[volumes.mounts]]
mountPath = "/app/data"
name = "chroma_db_storage"
